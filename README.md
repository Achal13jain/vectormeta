# vectormeta

Stop vector DB metadata limit errors before upsert.

`vectormeta` is a Python CLI for scanning vector records, finding oversized metadata, and moving heavy payload fields into sidecar JSON files.

This repository is in early MVP development.
